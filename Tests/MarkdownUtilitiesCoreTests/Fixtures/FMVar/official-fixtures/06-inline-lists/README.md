---
title: Localized inline lists
description: Illustrative locale-aware conjunction, disjunction, and unit list formatting.
---

# Localized inline lists

Operation: synchronize every reference in `before.md` once, producing `after.md`.

All references succeed using the document locale. The after file is an illustrative en-US Intl.ListFormat rendering, not a portable byte-exact oracle. Compare with the current formatter output; pin runtime and locale data for snapshots (Sections 2.2 and 3.8).
