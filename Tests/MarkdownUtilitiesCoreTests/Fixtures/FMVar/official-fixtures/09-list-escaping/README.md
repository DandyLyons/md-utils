---
title: Block list member escaping
description: Escaping HTML delimiters in block list members without introducing child
  markup.
---

# Block list member escaping

Operation: synchronize every reference in `before.md` once, producing `after.md`.

The list succeeds. Member markup is escaped; Markdown delimiters remain literal inside the HTML block list (Section 3.9).
