---
title: Ordered and unordered lists
description: Ordered and unordered HTML list caches with preserved sequence order,
  duplicates, and item coercion.
---

# Ordered and unordered lists

Operation: synchronize every reference in `before.md` once, producing `after.md`.

Both lists succeed. Sequence order and duplicates survive, and item coercion applies to each member. Block caches use HTML list structure (Sections 2.2 and 3.6).
